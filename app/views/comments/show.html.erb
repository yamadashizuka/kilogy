<p id="notice"><%= notice %></p>

<%= render 'show', comment: @comment %>

<p>
  <strong><%= t('activerecord.attributes.comment.topic_id') %>:</strong>
  <%= @comment.topic_id %>
</p>

<%= link_to 'Edit', edit_comment_path(@comment) %> |
<%= link_to 'Back', topics_path %>
